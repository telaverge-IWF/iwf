;config info for load test
;keys are callsPerSec, callDuration and loadTestDuration
;


callsPerSec = 5 ; Calls per Second

callDuration = 0 ; seconds between IAM(ANM) and REL

loadTestDuration = 99999 ; in Minutes ( 0 is infinite )


startCic = 1;
noCics = 500 ;
