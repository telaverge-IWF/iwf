//
// Generated by IntelliAsnCC ASN.1 compiler (C, C++, Java).
//
// Do not edit!
//
//
//
// (C) 2000, 2001 IntelliNet Technologies Inc. All Rights Reserved.
//
//


#include <map_v9_ruf_outcome.h>

#include <AsnDescEnumerated.h>


using namespace its;
using namespace map_v9;



AsnDescObject* RUF_Outcome::GetStaticDescription()
{
    static volatile bool initialized = false;
    static AsnDescObject* result = NULL;
    static AsnDescEnumerated staticDesc;

    if (initialized)
    {
        return result;
    }

    Asn::Lock();

    if (initialized)
    {
        Asn::Unlock();
        return result;
    }
    else
    {
        if (CheckLicense(ITS_LIC_CAP_GSMMAP) != ITS_TRUE)
        {
            Asn::Unlock();
            exit(0);
        }

        staticDesc.SetCloneForFactory(new RUF_Outcome(&staticDesc));
        staticDesc.TagAddLevel(new Tag(Tag::ASN_CLASS_UNIVERSAL, Tag::ASN_PRIMITIVE, 10));
        staticDesc._clauseExtensible = true;
        staticDesc._valueSet.insert(0);
        staticDesc._valueSet.insert(1);
        staticDesc._valueSet.insert(2);
        staticDesc._valueSet.insert(3);
        staticDesc._valueSet.insert(4);
        staticDesc._valueSet.insert(5);
        static AsnDescEnumerated::NamedNumber namedNumberList[] = {{"accepted", 0}, {"rejected", 1}, {"noResponseFromFreeMS", 2}, {"noResponseFromBusyMS", 3}, {"udubFromFreeMS", 4}, {"udubFromBusyMS", 5}, {NULL, 0}};
        staticDesc.SetNamedNumberList(namedNumberList);
        static AsnDescEnumerated::NamedNumber namedNumberExtList[] = {{NULL, 0}};
        staticDesc.SetNamedNumberExtList(namedNumberExtList);
        staticDesc.SetTypeName("RUF-Outcome");
        result = &staticDesc;
        initialized = true;
        Asn::Unlock();
        return result;
    }
}

long RUF_Outcome::Get(const std::string& valueName) const
{
    if (valueName == "accepted")
    {
        return 0;
    }
    else if (valueName == "rejected")
    {
        return 1;
    }
    else if (valueName == "noResponseFromFreeMS")
    {
        return 2;
    }
    else if (valueName == "noResponseFromBusyMS")
    {
        return 3;
    }
    else if (valueName == "udubFromFreeMS")
    {
        return 4;
    }
    else if (valueName == "udubFromBusyMS")
    {
        return 5;
    }
    else
    {
        bool invalid_value_name = false;
        ITS_ASSERT(invalid_value_name);

        return 0;
    }
}

std::string RUF_Outcome::Get(long value) const
{
    if (value == 0)
    {
        return "accepted";
    }
    else if (value == 1)
    {
        return "rejected";
    }
    else if (value == 2)
    {
        return "noResponseFromFreeMS";
    }
    else if (value == 3)
    {
        return "noResponseFromBusyMS";
    }
    else if (value == 4)
    {
        return "udubFromFreeMS";
    }
    else if (value == 5)
    {
        return "udubFromBusyMS";
    }
    else
    {
        bool invalid_value = false;
        ITS_ASSERT(invalid_value);

        return "";
    }
}

std::string RUF_Outcome::GetExt(long value) const
{
    return "";
}





