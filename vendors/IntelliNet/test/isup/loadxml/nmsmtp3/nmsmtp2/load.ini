;config info for load test
;keys are callsPerSec, callDuration and loadTestDuration
;


callsPerSec = 100 ; Calls per Second

callDuration = 0 ; seconds between IAM and REL

loadTestDuration = 10 ; in Minutes ( 0 is infinite )
