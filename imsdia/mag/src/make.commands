#
# change or override these settings based on J2SE installations
#
SABLECC=java -jar ../bin/sablecc.jar

JAVAC=javac -deprecation -O -classpath $(CLASSPATH)

JAVADOC=javadoc -classpath $(CLASSPATH)

JAR=jar

MKDIR=mkdir

CP=cp -f

RM=rm -f

FIND=find
